"use client";

import * as React from "react";

/**
 * AutoThemeProvider has been decommissioned in favor of SigitOS Theme System
 * (retro90s, dark, tokyo, vscode).
 */
export function AutoThemeProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
